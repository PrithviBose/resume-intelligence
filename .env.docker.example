# Copy to project root as `.env` for docker compose:
#   copy .env.docker.example .env
#
# Use your existing Postgres data directory so you keep current data.
POSTGRES_DATA_DIR=D:/Docker/PostgresData
